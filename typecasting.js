// string to Number

let contact = "0909090909"
console.log('contact type',typeof contact);

let convertedContact = +contact;    // if we want to change any string to number then just put "+" in front of that element
console.log('convertedContact', typeof convertedContact);

// boolean to string

let result = false;
console.log('result type', typeof result);

let convertedResult = result.toString();
console.log('convertedResult', typeof convertedResult);





